{
  "name": "juice-shop-security-inventory",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test analysis.test.mjs",
    "inventory": "node analyze.mjs --inventory-only",
    "analyze": "node --env-file-if-exists=../.env --env-file-if-exists=.env analyze.mjs"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.5.7",
    "typescript": "6.0.3"
  }
}
